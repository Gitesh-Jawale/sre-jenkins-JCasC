jenkins:
  systemMessage: "Welcome to Jenkins Configuration as a Code"

unclassified:
  location:
    url: "https://github.com/Gitesh-Jawale/sre-jenkins-JCasC.git/jenkins"

jobs:
  - script: |
      pipeline {
        agent any
        stages {
          stage('Build') {
            steps {
              sh 'echo "Building..."'
            }
          }
          stage('Test') {
            steps {
              sh 'echo "Testing..."'
            }
          }
          stage('Deploy') {
            steps {
              sh 'echo "Deploying..."'
            }
          }
        }
      }
